# Stackarr MCP configured

Stackarr now uses Hermes' native MCP client:

```bash
stackarr plugins install hermes --profile manage
```

Restart Hermes or start a new session. Destructive actions use Hermes' in-chat approval flow.
